name: libtab_examples
description: "Examples for pub.dev package libtab"
publish_to: 'none'
version: 1.0.0+1

environment:
  flutter: ">=3.32.0"
  sdk: '>=3.8.0 <4.0.0'

dependencies:
  cupertino_icons: ^1.0.8
  flutter:
    sdk: flutter
  libtab:
    path: ..

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true

